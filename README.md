# Mohaymen.GiteaClient
Mohaymen.GiteaClient is a nuget package for connecting and using gitea apis.

[!NOTE] 
In order to create tag in pipeline it needs your commit to starts with eighter "fix:" or "feat:"
